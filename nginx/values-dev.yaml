# values-dev.yaml

replicaCount: 1
image:
  repository: nginx
  tag: "1.16.0"
service:
  type: ClusterIP
  port: 80
resources:
  limits:
    cpu: 500m
    memory: 1Gi
  requests:
    cpu: 250m
    memory: 512Mi

environment: dev  # Environment added here
