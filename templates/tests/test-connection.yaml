apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tadvisor.fullname" . }}-test-connection"
  labels:
    {{- include "tadvisor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tadvisor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
